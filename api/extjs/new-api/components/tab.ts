'use strict';

const { queryCmpId } = require('../tia-extjs-query');

const compName = 'Tab';

const actions = {
  compName,
};

const checks = {
  compName,
};

const logs = {
  compName,
};

module.exports = {
  actions,
  checks,
  logs,
};
